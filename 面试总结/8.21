java面试题50道
1.什么是局部线程？
    线程局部变量是局限于线程内部的变量，属于线程私有的，不和其他线程共享。
    java提供了ThreadLocal来支持线程的局部变量，是一种线程安全的方式。但是一般使用需要注意，它的声明周期很长，任何线程局部变量一旦创建
    执行任务之后，没有释放，java应用就存在内存泄露的风险。

2.使用wait-notify写一段代码开解决生产者和消费者

3.java线程写一个单例模式？？

4.sleep和wait的区别？？
    （1）SLEEP不会释放锁,WAIT会释放锁
    （2）sleep方法到时间之后会自己执行，wait需要使用notify执行
        【补充】notify不会指定具体的sleep释放，这个由JVM决定
        notifyAll将线程变为就绪状态，不是变为运行状态，具体什么时候运行，这个需要自己竞争。

5.什么是不可变对象？java中怎样创建一个不可变的对象？？
    不可变对象是一旦创建，就不会发生改变，比如基本数据类型，那么就是值不会发生变化
    如果是引用数据类型，引用是不可以变的， 值是可以发生变化的 。

6.ArrayList和HashMap的默认值是多少？？
    java ArrayList默认是10个元素，HashMap是16（必须是2的幂此）

7.写一个容器，如果使用for-each进行遍历，那么就需要实现iterable接口，如果实现了collection接口，就自带了这个属性。

8.写一个ArrayList，如果想要删除一个元素？？
    问题的关键是：使用哪一个remove、

9.java的HashMap是如何工作的？？
    HashSet的内部是使用HashMap实现的。由于Map有key和value，所以所有的key有一个默认值value。因为HashMap的key只能有一个null，不可以重复
    那么HashSet的值不可以重复，只能有一个null值

10.HashTable和HashMap有什么不同
    （1）HashTable是jdk1.0的东西，HashMap是后来的东西
    （2）HashMap可以存入null建和值，HashTable不能有null建
    （3）HashTable线程是安全的，HashMap线程是不安全的

11、TreeSet是如果实现的？
    使用的是红黑树实现的

12.实现集合的排序
    使用排序的集合
    使用工具类collections的sort方法

13.ArrayList和LinkList的区别？？
    ArrayList底层是有数组实现的，所以可以随机访问，但是在中间插入删除比较慢
    LinkList 是由链表实现的，查找比较慢，但是插入和删除比较快。

14.java中LinkHashMap和PriorityQueue的区别是什么？？？？
    PriorityQueue保证最高或者最低优先级的元素总在队列的头部，但是LinkHashMap按照顺序，
    当遍历priorityQueue的时候，没有任何顺序保证

15.poll和remove的区别
    poll和remove都可以从队列中取出元素，但是poll获取失败返回空，remove返会的是异常

16.List/set/Map/Queue之间的区别
    List是一个有序集合，允许重复，它的某些实现可以提供基于下标值的常量访问

17.怎样将byte转化为String

18.Java中的编译1i常量是什么，使用它有什么风险??
    公共静态不可变变量就是编译其常量  这些其实在编译时会替换掉，因为编译器知道这些变量的值，并且知道他们是不会改的

    问题：这样的方式当你使用一个内部的，护着第三方公有的编译时常量，但是这个值被修改了，但是客户端扔是旧值，所以每次都需要重新编译程序

19.finally，final，finalize的区别？？
    finalize是垃圾回收机制，可以作为资源回收使用，他可以在对象回收之前执行，这也是对象唯一存活的机会。
    final可以修饰变量、类、方法，如果修饰的是引用变量，那么就是引用不可变，如果修饰的是基本数据类型，那么值不可以变
    如果修饰的是类，那么类不可以被继承，如果修饰方法，方法不可以被覆盖
    finally是异常中的一个模块，他是异常处理中一定会处理的部分，除了system.exit(0);

20.HashCode有什么用？？与a.equals(b)有什么答案？？
    他可以将不定长的字符串变为定长，常用于集合类hashMap，HashSet，HashTable
    它于equals的关系紧密，java中使用equal的时候，也需要使用hashcode

21.“==”和equal的区别？？
    == ：如果比较的是基本数据类型，那么比较的是他们的值，如果是引用数据类型，比较的是他们的引用
    equal比较的是他么的值

22.堆和栈的区别？？
    JVM中堆和栈属于不同的内存区域，使用的目的也是不同，栈用来存储局部变量和方法帧，速度也是比较块的
    对象的创建总是在堆上的，并且是共享的，栈是内存私有的。

23.GC不能保证及时的执行。

24.通过java命令启动的时候，会分配内存，内存的一部分作为堆内存，创建对象的时候会占用堆内存，这个时候垃圾回收期也会监控对象，当满足回收条件
的时候将其回收。

25.JRE：运行环境,java说所必须的
   JDK: 开发工具包
   JVM：Java虚拟机，运行java程序
   JIT：代表即时编译，当代码编译超过阈值的时候，会将字节码转换为本代码

26.为什么String是不可变的
    因为String使用了final修饰，String类型是比较常用的，设为不可变的，可以允许对个客户端之间共享相同字符串











































