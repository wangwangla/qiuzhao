1.cookie和session的区别：
（1）cookie存在于客户端  session在放在服务端
（2）session一般用来做验证，在的服务端比较安全，但是存储大量的信息会使得服务器端占用大量的内存，所以安全性不高的。可以使用cookie
（3）session依赖于sessionId来实现服务端对于客户端的识别
    sessionid传递有两种，一种是通过cookie传递  缺点：如果客户端禁用了cookie，session也无法使用。
    还有一种是通过url传递的。
（4）单个cookie不超过4k,一般都会限制这个个数

2.Jsp和Servlet的区别：
Servlet是一种特殊的Java程序，它运行于服务端的JVM中，能够依靠服务端的支持向浏览器宣誓内容，JSP本质上是Servlet的一直能够简化方式，JSP类似于Servlet程序，简化页面的生成，Servlet和jsp 最不同的是Servlet注重的是逻辑，jsp注重的是页面显示效果，在MVC中，jsp用于显示，Servlet注重于逻辑

http是无状态的，所以在请求的时候，客户端第一次访问服务器端时候，会分配一个id，每次客户端访问时候，带上它，用来标识身份
有两种方式可以实现：将参数放到隐藏域中   重写URL

3.hibernate的优化策略
    （1）指定合理的缓存
    （2）采用合适的session管理机制
    （3）尽量使用延迟加载
    （4）设置合理的处理参数
    （5）如果可以采用UUID主键
    （6）如果可以采用乐观锁代替悲观锁
    （7）合理索引，恰当的分区

4.一级缓存和二级缓存
    一级缓存和二级缓存是对整个实体进行的缓存，不会缓存