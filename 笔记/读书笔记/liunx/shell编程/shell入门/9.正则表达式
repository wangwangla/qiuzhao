正则表达式
正在表达式的基础:
    定义：使用简答的字符按照原先设定的规则来完成复杂的功能
    是一种字符串的匹配方式，其实就是一个匹配摸版

1.正则表达式的分类：   
    两种引擎：
        posix基本正则表达式引擎
        posix扩展正则表达式引擎
    正则表达式引擎来执行正则，使用的方式也是不一样的，

2.正则表达式的常用字符
    （1）.当前为单个字符                h.llo
    （2）^定位符,开头是XXX              [^XXX]
        ^：特殊的时候使用#和^一起       #\^  一起表示^开头的字符
    （3）$匹配行尾
    =======================================================
    （1）*匹配字符串单个或者其重复序列  a*、*a、a*b
    （2）\屏蔽掉一些特殊字符的含义
3.扩展正则表达式的常用符号
    （1）[]:匹配一个范围或者一个集合
        [a-c]：a、b、c【闭区间】
    （2）\{\}：匹配模式结果出现的次数
        string\{n\} :string出现n次
        string\{n,\}m：string至少出现n次
        string\{n,m\}：string出现大于n次，小于m次
    （3）？:出现一次或者没有出现的








[目录](https://github.com/wangwangla/biji/tree/master/README.md)